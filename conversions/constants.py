# Putting file paths in a constant so things still work if we change up the file structure
ACTIVITY_FILE_PATH = './activities'
CONVERSION_OUTPUT_PATH = './conversions/outputs'

SUPPORTED_FILE_FORMATS = ('tcx', 'gpx', 'fit')
