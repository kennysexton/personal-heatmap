# Generate a running heatmap using activity data files


### Build .geojson used for heatmap from a .gpx (wearable) file
`pip install gpxpy`
`pip install geojson`

then run

`python gpx_to_geojson.py`